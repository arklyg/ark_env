[user]
    name = your_name
    email = your_email

[alias]
    st = status
    ci = commit -a --no-verify
    pu = push origin master
    di = diff
    co = checkout

[core]
    excludesfile = ~/.gitignore
